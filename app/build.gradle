plugins {
    id "com.android.application"
}

android {
    namespace "com.osmo.preview"
    compileSdk 35

    defaultConfig {
        applicationId "com.osmo.preview"
        minSdk 26
        targetSdk 35
        versionCode 2
        versionName "v0.0.2"
    }
}
